import Root, { type MarqueeDirection, type MarqueeProps } from './marquee.svelte';
import Item, { type MarqueeItemProps } from './marquee-item.svelte';

export {
	Root,
	Item,
	type MarqueeDirection,
	type MarqueeProps,
	type MarqueeItemProps,
	//
	Root as Marquee,
	Item as MarqueeItem
};
